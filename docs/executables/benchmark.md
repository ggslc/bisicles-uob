# BISICLES solver benchmark executable

The `solverBenchmark` executable is used to run the ice velocity solver as a standalone tool.

For detailed information about the solver benchmark, see [benchmark.html](../benchmark.html).
